<Project>

  <PropertyGroup>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <CentralPackageTransitivePinningEnabled>true</CentralPackageTransitivePinningEnabled>
  </PropertyGroup>

  <ItemGroup Label="Test stack">
    <PackageVersion Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageVersion Include="xunit" Version="2.9.2" />
    <PackageVersion Include="xunit.runner.visualstudio" Version="2.8.2" />
    <PackageVersion Include="Bogus" Version="35.6.1" />
    <PackageVersion Include="coverlet.collector" Version="6.0.2" />
  </ItemGroup>

  <ItemGroup Label="Source link">
    <PackageVersion Include="Microsoft.SourceLink.GitHub" Version="8.0.0" />
  </ItemGroup>

  <ItemGroup Label="Cross-repo Tamp packages — wrappers consumed from their own satellite repos. The Wave 1+2 security chain was split out of this monorepo in TAM-254; from 1.12.0 onward the security wrappers are PackageReferences from their satellites, and Tamp.Security.Pipeline references them the same way.">
    <PackageVersion Include="Tamp.SonarScanner.V10" Version="0.1.0" />
    <PackageVersion Include="Tamp.CycloneDx.V6" Version="1.11.2" />
    <PackageVersion Include="Tamp.OpenGrep" Version="1.11.2" />
    <PackageVersion Include="Tamp.OsvScanner.V2" Version="1.11.2" />
    <PackageVersion Include="Tamp.Trivy" Version="1.11.2" />
    <PackageVersion Include="Tamp.DependencyTrack.V1" Version="1.11.2" />
    <PackageVersion Include="Tamp.DefectDojo.V2" Version="1.11.2" />
  </ItemGroup>

  <ItemGroup Label="Core dependencies">
    <PackageVersion Include="Microsoft.Extensions.FileSystemGlobbing" Version="9.0.0" />
  </ItemGroup>

  <ItemGroup Label="Analyzer (TAM-175 part a)">
    <PackageVersion Include="Microsoft.CodeAnalysis.CSharp" Version="4.8.0" />
    <PackageVersion Include="Microsoft.CodeAnalysis.Analyzers" Version="3.11.0" />
  </ItemGroup>

  <ItemGroup Label="Security analyzers — Roslyn SARIF leg of Wave 1 chain (TAM-248). Activated per build via /p:IncludeSecurityAnalyzers=true; idle on default Compile.">
    <PackageVersion Include="SonarAnalyzer.CSharp" Version="10.25.0.139117" />
    <PackageVersion Include="Roslynator.Analyzers" Version="4.15.0" />
  </ItemGroup>

</Project>
